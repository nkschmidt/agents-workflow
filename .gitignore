# Локальный OpenCode-сетап и генерируемые из .claude/agents/ файлы.
# Источник истины для агентов — .claude/agents/.
.opencode/

# Локальные машинные оверрайды Claude Code (не в репо, апдейтер не трогает).
# Общий framework-baseline живёт в .claude/settings.json.
.claude/settings.local.json

.DS_Store
